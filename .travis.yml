dist: xenial
language: python
python:
  - "3.7"

matrix:
  fast_finish: true

install:
  - pip install pipenv --upgrade
  - pipenv install
  - pipenv sync
  - pipenv sync --dev

script:
  - pipenv run pylama
  - pipenv run pytest

notifications:
  email: false
